/** * Created by Samvel Abrahamyan on 8/28/16. */"use strict";var AbstractWorld = require('../main/AbstractWorld');var chai = require('chai');var world = new AbstractWorld({    rows: 10,    cols: 20});describe('World', () => {        describe('construction', () => {        it('should create grid with correct sizes', () => {            chai.assert.equal(world._grid.length, 10);            world._grid.forEach((row) => {                chai.assert.equal(row.length, 20);            })        })    });    describe('addPlayer', () => {        it('should create the player', () => {            var player = world.addPlayer();            chai.assert.equal(player.playerId, world._lastPlayerId);            player.positions.forEach((pos) => {                chai.assert.equal(world.getPlayerIdAt(pos), player.playerId);                chai.assert.equal(world.getShapeIdAt(pos), pos.shapeId);            })        })    })        });